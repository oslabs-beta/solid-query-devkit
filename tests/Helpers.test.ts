import { render, waitFor } from '@solidjs/testing-library';
import { sortQueries, getQueryStatus, filterQueries } from '../src/Helpers'
import '@solidjs/testing-library';
import '@testing-library/jest-dom'

describe('sortQueries functions as expected', () => {

  // next steps:
  // - write test queries
  // - design tests for the helper functions

})